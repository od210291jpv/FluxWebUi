[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "flux-webui-backend"
version = "0.1.0"
description = "FastAPI backend for Flux Web UI"
requires-python = ">=3.11"
